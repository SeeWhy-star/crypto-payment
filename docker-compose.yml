services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: crypto_payment
      MYSQL_USER: crypto_payment
      MYSQL_PASSWORD: ${DB_PASSWORD:-change-me}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-change-root-me}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
